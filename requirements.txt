# Install GDAL first if you are using pip rather than conda. The C-extension
# packages must be installed before geopandas, and getting that order wrong
# produces errors that look like something else entirely. environment.yml is
# the easier path.

# Spatial core
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
rasterio>=1.3
fiona>=1.9
rtree>=1.2

# Databases
psycopg[binary]>=3.2
sqlalchemy>=2.0
geoalchemy2>=0.15
duckdb>=1.1

# Data access
pyrosm>=0.6
requests>=2.32
pystac-client>=0.8
planetary-computer>=1.0
odc-stac>=0.3

# Formats
pyarrow>=17.0
pandas>=2.2
numpy>=1.26

# Configuration and output
pyyaml>=6.0
python-dotenv>=1.0
h3>=4.1

# Notebooks and quick visualisation
folium>=0.17
matplotlib>=3.9
jupyter>=1.1

# Development
ruff>=0.6
pytest>=8.3
